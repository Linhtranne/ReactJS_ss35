import React from 'react'

export default function index() {
  return (
    <div>index
        {/* 
        useState = use state
        trạng thái của dữ liệu tức là khi dữ liệu thay đổi thì giao diện nó thay đổi theo
        các hook là các phương thức được React cung cấp cho nên khi sử dụng phải import vào để dùng
        bản chất useState là 1 function trả về 1 cái [] có 2 phần tử
        khi setCount thì component re-render lại
        đối với kĩ thuật batch-update thì nó sẽ lấy giá trị trả về của useState trước khi làm giá trj tham số đầu vào của useState sau 
        */}
    </div>
  )
}
